apiVersion: v1
kind: Service
metadata:
    name: "datateam-0087"
    labels:
        app: "datateam-0087"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "datateam-0087"