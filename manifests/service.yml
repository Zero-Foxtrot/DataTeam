apiVersion: v1
kind: Service
metadata:
    name: "datateam-98fe"
    labels:
        app: "datateam-98fe"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "datateam-98fe"